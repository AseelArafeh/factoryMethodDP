
package factorymethoddesignpattern;


public interface FileParser {
    
    String parseFile(String originPath);
    // parseFile method: takes a file path, and returns a result message for user.
    
}
